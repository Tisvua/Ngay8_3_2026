/* ============================================
   PAGE TRANSITION — simple redirect, no animation
   ============================================ */

window.navigateWithCar = function (url) {
    window.location.href = url;
};
